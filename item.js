/**Store all command line parameters in a separate object */
const args = process.argv.splice(2);

/**
 * For an item to be generated we can give it
 * - Category (weapon, potion, scroll, etc)
 * - Rarity ()
 */